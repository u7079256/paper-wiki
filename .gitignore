# --- secrets: NEVER commit credentials -------------------------------------
.env
.env.*
*.secret
**/MINERU_REMOTE_PASS*
# Client-local notes live outside this repo. Memory templates here must never
# contain passwords or other secrets.

# --- python ----------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/

# --- OS / editor junk ------------------------------------------------------
.DS_Store
._*
__MACOSX/
Thumbs.db
.idea/
.vscode/
.obsidian/workspace*

# --- safety net: source material & OCR output are heavy and may be private --
# (these only appear if you run a wiki project INSIDE this repo, which you should
#  not -- bootstrap creates projects elsewhere. Ignored just in case.)
*.pdf
*.pptx
**/mineru/
