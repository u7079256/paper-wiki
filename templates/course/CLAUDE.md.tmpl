> 🚧 **TODO (delete this block after /wiki-init):** 由 bootstrap 从 claude-wiki-builder 的 **course** 变体生成。
> 通用规则(raw→wiki 两层、OCR 管线)直接沿用;只需改:① 顶部课程描述 ② 「范围」如何界定(哪份 review/syllabus 文档)③ 配合 research.md 填材料清单。

# Course Wiki — {{PROJECT_NAME}}

> 这是一个 **LLM Wiki**(Claude Code 逐页读材料、**编译**结构化知识),不是 RAG。
>
> **本项目是一门课程的知识 / 复习库,不是研究课题。** 把每份 slide / lab / assignment 当作「文献」编译成结构化笔记:每讲一篇 → 跨讲做主题综合。
>
> - **范围**:_(可选)由一份 review / syllabus 文档界定(如 `Review lecture.pdf`)—— 编译不超出该范围,out-of-scope 不留存。_ 详见 `wiki/exam-scope.md`(读完范围文档后生成)。
> - **资源优先级**:lecture slide > workshop / lab > assignment。编译精力按此分配。
> - 详见 `research.md`(进度主线)。

## 数据架构:raw/(只增不改)→ wiki/(可重写)

```
{{TOPIC}}-wiki/
├── raw/                      # 源材料(只读)
│   └── {{TOPIC}}/
│       ├── lecture-slides/     # 讲义(最高优先级)
│       ├── lab/                # workshop 题+解
│       ├── assignment*/        # 作业(可含代码,只读)
│       └── mineru/             # OCR 出的 markdown(派生源)
├── wiki/                     # LLM 编译产物
│   ├── exam-scope.md           # ★脊柱:范围文档提炼的考点/重点总清单 + 索引(可选)
│   ├── lectures/               # 一篇 = 一讲的笔记
│   ├── topics/                 # 跨讲主题综合(≥3 篇同主题再合;可含 deep-dives/)
│   ├── practice/               # lab / 作业的题+解,按考点索引(高价值)
│   └── notes/                  # 会话快照
├── research.md               # 进度主线 & 上下文
├── scripts/                  # 远程 OCR + PPTX 兜底
└── .claude/{commands,agents}/
```

> **不预先建空目录** —— `lectures/` `topics/` `practice/` 在产出第一篇时再建。

## 命令(研究专用的已退役)

| 命令 | 作用 |
|---|---|
| `/wiki-compile` | 把 raw/ 新材料编译成 lecture / topic / practice 笔记 |
| `/wiki-ask <问题>` | 只读查询已编译 wiki,逐条标出处,没有的说没有 |
| `/wiki-critique <文件>` | 对抗式查错(公式 / 结论是否写错) |

> 课程库**不向外扩展**,故 `wiki-search-latest` / `wiki-verify-novelty` 及其 agent 未安装。`/wiki-critique` 可 spawn `wiki-critic` 查错。

## Claude Code 编译规则(`/wiki-compile`)

### 1. 扫描 raw/
列出 `raw/{{TOPIC}}/mineru/` 下 OCR markdown(+ lab 的 ipynb),与 `wiki/lectures/`·`practice/` diff。**先用范围文档 / `exam-scope.md` 判 in-scope**:out-of-scope 不编译进 wiki/(raw 保留 OCR 当归档)。

### 2. lecture 笔记 (`wiki/lectures/<id>.md`)
> 无 authors / venue / arXiv —— 这是讲义不是论文。
```markdown
---
title: <讲题 / 材料名>
source: <raw/{{TOPIC}}/lecture-slides/X.pdf, slides N–M>
scope: in | partial | out
importance: high | med | low      # slide=high · lab=med · assignment=low 起步
compiled_at: <YYYY-MM-DD>
---
## 重点速览        <!-- 这一讲哪些点最重要 / 可能考,1–3 条 -->
## 核心概念        <!-- 定义、直觉 -->
## 关键公式        <!-- LaTeX,标清每个符号 -->
## 推导 / 方法     <!-- 忠实于 slide 的关键推导 -->
## 例子 / 常见陷阱
## 相关练习 → [[practice-xxx]]
## 出处            <!-- slide 页码 -->
```

### 3. topic 综合 (`wiki/topics/<topic>.md`)
≥3 篇 lecture 共享一主题时生成:
```markdown
---
type: topic
scope: in | partial | out
related_lectures: [[lecture-a]], [[lecture-b]], [[lecture-c]]
---
## 全景
## 核心方法族(按方法分类,不按讲分类)
## 公式 / 推导汇总
## 易混点 / 对比
## 相关练习 → [[practice-xxx]]
```

### 4. practice 笔记 (`wiki/practice/<id>.md`)
```markdown
---
type: practice
source: <raw/{{TOPIC}}/lab/Week N questions.pdf (+ sol)>
topics: [[topic-or-lecture]], ...
---
## 题目
## 解答     <!-- 忠实于所给 solution;无官方解时标「未提供官方答案,仅整理题目」 -->
## 用到的概念 → [[topic-or-lecture]]
```

### 5. `wiki/exam-scope.md`(脊柱,可选)
读完范围文档后生成:每个重点 → 链到对应 lecture / topic / practice + 标 `in | partial | out`。既是复习总览,也是「该不该编译 / 保留」的裁决依据。

### 6. 反链 / Lint / 禁止
- 引用用 `[[filename]]`;lecture 列「相关练习」,topic frontmatter 列 `related_lectures`。
- 矛盾检测(同 topic 多讲矛盾 → 写 `## 易混点`);孤立文件标 `tags: [orphan]`;`compiled_at` 用实际日期。
- ❌ 改 `raw/` / ❌ 复制 raw 原文进 wiki / ❌ 写没读过的材料 / ❌ 空洞套话(每句要有 slide 出处)/ ❌ 编译 out-of-scope / ❌ lab 无官方答案时编造解法(只整理题目)。

## 远程 OCR 入库管线

材料 PDF → Markdown 的 OCR **必须在远程 GPU 上跑**,本地禁用。**配置走环境变量(密码绝不入库)**:
```
$env:MINERU_REMOTE_HOST = "<你的 GPU 服务器 host/IP>"
$env:MINERU_REMOTE_USER = "<ssh 用户>"
$env:MINERU_REMOTE_PASS = "<密码,只存本地 memory>"
python scripts/mineru_remote_ocr.py [input_dir]   # 默认 raw/{{TOPIC}}/
```
产物落 `raw/{{TOPIC}}/mineru/`。OCR namespace = `mineru_{{NS}}_*`,与其他项目隔离。

### 🔴 硬规则 / 已知坑
- ❌ CPU fallback / ❌ 本地跑 mineru / ❌ 换服务器。GPU 占用时干净退出(退出码 4),不轮询、不 CPU fallback、不 kill 别人进程。
- 脚本只抓 input_dir **直接子级** `*.pdf`(不递归)—— PDF 在 `lecture-slides/` 等子目录时先平铺到临时目录再喂(文件名不重名时无冲突)。
- **PPTX** mineru 不吃:**优先远程 `soffice --headless --convert-to pdf` 转 PDF 再走同一管线**(质量与其余 slide 一致);本地兜底 `scripts/extract_pptx.py`(版式丢失,需 `PYTHONIOENCODING=utf-8`)。
- 共享 GPU/conda env 时**两项目别同时跑 OCR**。
- ⚠️ **Windows GBK 坑**:批量改 wiki 中文文件**务必走 Read/Edit/Write 或 .NET UTF-8**,别用裸 `Get-Content`/`Set-Content`(中文 Windows 按 GBK 会损坏)。详见 skill `docs/GOTCHAS.md`。

### 凭据
密码**仅存本地 memory**,脚本读环境变量。memory 模板见 skill `templates/memory/remote-ocr-gpu-server.md.tmpl`,填好放进本项目 memory,**永不进 repo**。

## 课程代码

课程自带代码(如 `raw/{{TOPIC}}/assignment*/...`)视同 `raw/` 只读源。`/wiki-compile` 及子 agent **不自动扫描** raw 下 `.py` / `.ipynb` / checkpoint;除非用户明确要求「读某文件」。结论由对话提炼后写进 wiki,不反向。

## research.md 的作用

多轮对话共享上下文(进度 / 材料清单 / TODO)。开新对话时**先读 research.md** 再动手。
